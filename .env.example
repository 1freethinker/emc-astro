# Copy to `.env` for local dev, and set the same key in Netlify
# (Site settings -> Environment variables). It's a public submission key.
#
# Get one at https://web3forms.com — enter the address that should receive
# contact-form messages; Web3Forms emails you the access key.
PUBLIC_WEB3FORMS_KEY=
